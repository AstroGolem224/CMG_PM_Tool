# CMG PM Tool Backend

FastAPI + SQLModel backend for the CMG PM Tool.

## Quick Start

```bash
python -m venv .venv
. .venv/bin/activate
pip install -e .[dev]
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```
